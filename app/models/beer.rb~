class Beer < ActiveRecord::Base
  belongs_to :brewery
  has_many :ratings, dependent: :destroy

  def average_rating
    if ratings.count > 0
      ratings.average(:score)
    end
  end

  def to_s
    "#{name}, #{brewery.name}"
  end
end
